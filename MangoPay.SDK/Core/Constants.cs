
namespace MangoPay.SDK.Core
{
    /// <summary>Represents a container for constant values.</summary>
    public static class Constants
    {
        /// <summary>Resolves to "application/json" string.</summary>
        public const string APPLICATION_JSON = "application/json";

        /// <summary>Resolves to "Content-Type" string.</summary>
        public const string CONTENT_TYPE = "Content-Type";

        /// <summary>Resolves to "application/x-www-form-urlencoded" string.</summary>
        public const string APPLICATION_X_WWW_FORM_URLENCODED = "application/x-www-form-urlencoded";

        /// <summary>Resolves to "Authorization" string.</summary>
        public const string AUTHORIZATION = "Authorization";

        /// <summary>Resolves to "?" string.</summary>
        public const string URI_QUERY_SEPARATOR = "?";

        /// <summary>Resolves to ampersand sign.</summary>
        public const string URI_QUERY_PARAMS_SEPARATOR = "&";

        /// <summary>Resolves to "X-Number-Of-Pages" string.</summary>
        public const string X_NUMBER_OF_PAGES = "X-Number-Of-Pages";

        /// <summary>Resolves to "X-Number-Of-Items" string.</summary>
        public const string X_NUMBER_OF_ITEMS = "X-Number-Of-Items";

		/// <summary>Resolves to "User-Agent" string.</summary>
		public const string USER_AGENT = "User-Agent";

		/// <summary>Resolves to "Link" string.</summary>
		public const string LINK = "Link";

        /// <summary>Resolves to "Status" string.</summary>
        public const string STATUS = "Status";

        /// <summary>Resolves to "Type" string.</summary>
        public const string TYPE = "Type";

        /// <summary>Resolves to "Nature" string.</summary>
        public const string NATURE = "Nature";

        /// <summary>Resolves to "Direction" string.</summary>
        public const string DIRECTION = "Direction";

        /// <summary>Resolves to "BeforeDate" string.</summary>
        public const string BEFOREDATE = "BeforeDate";

        /// <summary>Resolves to "AfterDate" string.</summary>
        public const string AFTERDATE = "AfterDate";

        /// <summary>Resolves to "eventtype" string.</summary>
        public const string EVENTTYPE = "eventtype";

        /// <summary>Resolves to ".tmp" string.</summary>
        public const string TMP_FILE_EXTENSION = ".tmp";

        /// <summary>Resolves to "grant_type" string.</summary>
        public const string GRANT_TYPE = "grant_type";

        /// <summary>Resolves to "client_credentials" string.</summary>
        public const string CLIENT_CREDENTIALS = "client_credentials";

        /// <summary>Resolves to "Host" string.</summary>
        public const string HOST = "Host";

        /// <summary>Resolves to "Basic" string.</summary>
        public const string BASIC = "Basic";

        /// <summary>Resolves to "ClientId" string.</summary>
        public const string CLIENT_ID = "ClientId";

        /// <summary>Resolves to "Name" string.</summary>
        public const string NAME = "Name";

        /// <summary>Resolves to "Email" string.</summary>
        public const string EMAIL = "Email";

        /// <summary>Resolves to "_" string.</summary>
        public const string SORT_FIELD_SEPARATOR = "_";

        /// <summary>Resolves to "Sort" string.</summary>
        public const string SORT_URL_PARAMETER_NAME = "Sort";

        /// <summary>Resolves to "first" string.</summary>
        public const string LINKS_FIRST_ITEM = "first";

        /// <summary>Resolves to "next" string.</summary>
        public const string LINKS_NEXT_ITEM = "next";

        /// <summary>Resolves to "prev" string.</summary>
        public const string LINKS_PREVIOUS_ITEM = "prev";

        /// <summary>Resolves to "last" string.</summary>
        public const string LINKS_LAST_ITEM = "last";

	/// <summary>Resolves to "DisputeType" string.</summary>
	public const string DISPUTE_TYPE = "DisputeType";

	/// <summary>Resolves to "Idempotency-Key" string.</summary>
	public const string IDEMPOTENCY_KEY = "Idempotency-Key";

		/// <summary>Resolves to "ResultCode" string.</summary>
		public const string RESULT_CODE = "ResultCode";

		/// <summary>Resolves to "MinDebitedFundsAmount" string.</summary>
		public const string MIN_DEBITED_FUNDS_AMOUNT = "MinDebitedFundsAmount";

		/// <summary>Resolves to "MinDebitedFundsCurrency" string.</summary>
		public const string MIN_DEBITED_FUNDS_CURRENCY = "MinDebitedFundsCurrency";

		/// <summary>Resolves to "MaxDebitedFundsAmount" string.</summary>
		public const string MAX_DEBITED_FUNDS_AMOUNT = "MaxDebitedFundsAmount";

		/// <summary>Resolves to "MaxDebitedFundsCurrency" string.</summary>
		public const string MAX_DEBITED_FUNDS_CURRENCY = "MaxDebitedFundsCurrency";

		/// <summary>Resolves to "UserId" string.</summary>
		public const string USER_ID = "UserId";

		/// <summary>Resolves to "WalletId" string.</summary>
		public const string WALLET_ID = "WalletId";
    }
}
